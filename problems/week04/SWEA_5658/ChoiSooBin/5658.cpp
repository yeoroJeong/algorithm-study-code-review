/*
처음에는 문자열을 직접 회전시키는 방식도 생각했지만,
실제로는 회전된 문자열 전체를 만들 필요 없이 시작 위치만 한 칸씩 옮겨가며 각 변의 값을 계산하면 충분했다.

또한 같은 값이 여러 회전에서 중복으로 만들어질 수 있으므로,
중복 제거와 내림차순 정렬을 동시에 처리할 수 있는 set<int, greater<int>>를 사용했다.

SWEA 실행시간 및 메모리
언어        C++
메모리      5,836 kb
실행시간    7 ms
코드길이    1,296
결과        Pass

문제: 5658. [모의 SW 역량테스트] 보물상자 비밀번호
링크: https://swexpertacademy.com/main/code/problem/problemDetail.do?contestProbId=AWXRUN9KfZ8DFAUo
작성자: 최수빈(sbyy77dev)
알고리즘: 완전 탐색, Set

[문제 요약]
N개의 16진수 숫자가 원형으로 배치된 보물상자가 주어진다.
보물상자는 한 번 회전할 때마다 모든 숫자가 시계 방향으로 한 칸씩 이동한다.

각 변에는 N/4개의 숫자가 있으며, 시계 방향 순서대로 하나의 16진수를 만든다.
보물상자를 회전하면서 만들 수 있는 모든 숫자 중 중복을 제외하고,
K번째로 큰 값을 10진수로 변환하여 출력해야 한다.

[핵심 아이디어]
한 변의 길이는 N/4이므로 N/4번 회전하면 다시 처음과 동일한 숫자 조합이 만들어진다.
따라서 실제로 확인해야 하는 회전 횟수는 N/4번뿐이다.

입력받은 16진수 문자를 0~15의 정수로 변환해 배열에 저장한다.
이때 배열을 입력 문자열과 반대 방향으로 저장해 각 자리의 값을 16^j와 바로 곱할 수 있도록 했다.

원형 배열의 끝을 넘어가는 경우를 편하게 처리하기 위해
배열 뒤에 처음 N/4개의 값을 한 번 더 복사한다.
그러면 별도의 나머지 연산 없이 시작 위치를 i만큼 이동하면서
각 변의 숫자를 연속된 구간처럼 계산할 수 있다.

각 회전마다 4개의 변에서 만들어지는 숫자를 모두 구한 뒤
set<int, greater<int>>에 삽입한다.
set은 같은 값이 다시 삽입되어도 중복 저장하지 않고,
greater<int>를 사용했기 때문에 자동으로 내림차순으로 정렬된다.

모든 경우를 저장한 후 set의 begin()부터 K-1번 이동하면
K번째로 큰 숫자를 바로 얻을 수 있다.

[풀이 과정]
1. N과 K, N자리의 16진수 문자열을 입력받는다.
2. 문자열의 각 문자를 정수 값으로 변환한다.
   - '0'~'9'는 문자에서 '0'을 빼서 0~9로 변환한다.
   - 'A'~'F'는 문자에서 'A'를 빼고 10을 더해 10~15로 변환한다.
3. 각 숫자의 자릿수 계산을 편하게 하기 위해 입력 순서와 반대로 hex 배열에 저장한다.
4. 원형 배열 처리를 위해 hex 배열 뒤쪽에 처음 N/4개의 값을 추가로 복사한다.
5. 회전 횟수 i를 0부터 N/4-1까지 순회한다.
6. 각 회전에서 4개의 변을 확인한다.
7. 한 변의 N/4개 숫자를 순회하며
   각 값에 16^j를 곱해 더하여 해당 변의 16진수를 10진수 값으로 계산한다.
8. 만들어진 값을 set<int, greater<int>>에 삽입한다.
   같은 값이 이미 존재하면 중복으로 저장되지 않는다.
9. 모든 회전이 끝나면 set은 서로 다른 값들을 내림차순으로 가지고 있다.
10. begin() iterator에서 K-1번 이동해 K번째 값을 찾는다.
11. iterator가 가리키는 값을 정답으로 출력한다.

[복잡도]
- 시간 복잡도: 회전은 N/4번 수행하고, 각 회전마다 4개의 변을 확인한다.
  한 변의 숫자는 N/4개이므로 숫자를 생성하는 연산 자체는
  (N/4) * 4 * (N/4) = O(N^2)이다.

  만들어지는 숫자는 최대 N개이며 각 값을 set에 삽입할 때 O(log N)이 필요하므로
  전체적으로 O(N^2 log N) 정도로 볼 수 있다.
  N은 최대 28이므로 실제 연산량은 매우 작다.

- 공간 복잡도: 입력된 숫자를 저장하는 hex 배열은 O(N) 크기이고,
  생성 가능한 서로 다른 숫자를 저장하는 set 역시 최대 O(N)개의 값을 저장한다.
  따라서 전체 공간 복잡도는 O(N)이다.

[막혔던 부분과 오답 원인 및 해결 방법]
- 첫 번째 삽질 (set과 string 변수 이름 충돌):
  처음에는 중복 제거용 set의 변수명을 s로 선언했는데,
  이후 입력 문자열 역시 string s로 선언했다.
  이 때문에 s.insert(num)을 호출했을 때 set의 insert가 아니라
  string의 insert 함수로 인식되어 인수 목록이 일치하지 않는다는 빌드 에러가 발생했다.
  -> set의 이름을 mxk로 변경해 두 변수가 겹치지 않도록 수정했다.

- 두 번째 삽질 (set의 K번째 원소 접근):
  set은 vector처럼 [] 연산자를 통한 인덱스 접근이 불가능하다.
  처음에는 begin()에서 K번 iterator를 이동하려 했지만,
  begin() 자체가 이미 첫 번째 원소를 가리키고 있기 때문에 한 칸을 더 이동하게 된다.
  -> begin()에서 K-1번 iterator를 증가시켜 K번째 원소를 찾도록 수정했다.

- 세 번째 삽질 (각 변의 시작 인덱스 계산):
  각 변을 확인할 때 처음에는 side*n을 사용해 다음 변으로 이동하려 했다.
  하지만 한 변의 길이는 N이 아니라 N/4이므로 배열 범위를 크게 벗어나게 되고
  Debug Assertion Failed가 발생할 수 있었다.
  -> 각 변의 시작 위치를 side*(N/4)로 계산하도록 수정했다.

- 네 번째 삽질 (원형 배열 처리):
  회전할 때 배열의 끝을 넘어 다시 처음으로 돌아가는 처리를 어떻게 할지 고민했다.
  매번 인덱스에 %N을 적용할 수도 있지만 계산식이 복잡해질 수 있었다.
  -> 배열 뒤에 처음 N/4개의 값을 복사해두고,
     회전 위치 i를 시작 인덱스에 더하는 방식으로 원형 배열을 선형 구간처럼 처리했다.

- 다섯 번째 삽질 (16진수 문자 변환):
  입력은 문자열이기 때문에 'A'~'F'를 그대로 정수 계산에 사용할 수 없었다.
  -> '0'~'9'는 s[i]-'0',
     'A'~'F'는 s[i]-'A'+10으로 변환하여 0~15의 값으로 저장했다.

[추가로 궁금했던 점]
- 16진수도 비트마스킹이 가능한가?
  가능하다. 16진수 한 자리는 정확히 4bit이므로
  예를 들어 1A3은 내부적으로 0001 1010 0011과 같이 표현할 수 있다.

  따라서 여러 16진수 자릿수를 하나의 정수에 저장할 때
  기존 값을 4bit 왼쪽으로 이동한 뒤 새로운 값을 OR 하는 방식도 사용할 수 있다.

  num = (num << 4) | hexValue;

  이 문제에서 한 변의 최대 길이는 7이고 최대값은 FFFFFFF = 16^7-1 = 2^28-1이므로
  32bit int 범위 안에 충분히 들어간다.
*/
#include<iostream>
#include<climits>
#include<algorithm>
#include<string>
#include<set>
#include<cmath>


using namespace std;
typedef long long ll;
int mx, mn;

int main(int argc, char** argv)
{
    int test_case;
    int T;
    int hex[35];
    
    cin >> T;

    for (test_case = 1; test_case <= T; ++test_case)
    {
        set<int, greater<int>> mxk;    // 내림차순
        mx = INT_MIN;
        mn = INT_MAX;
        fill(hex, hex + 35, 0);
        int ans = 0;
        int n, k;
        string s;
        // input
        cin >> n >> k;
        cin >> s;
        for (int i = 0; i < s.size(); i++) {
            if ('0' <= s[i] && s[i] <= '9') {
                hex[n - i - 1] = s[i] - '0';
            }
            else {    // 문자
                hex[n - i - 1] = s[i] - 'A' + 10;
            }
        }
        for (int i = 0; i < n / 4; i++) {
            hex[n + i] = hex[i];
        }
        // solve
        
        for (int i = 0; i < n / 4; i++) {    // 회전
            for (int side = 0; side < 4; side++) {    // 네 변의 숫자 set에 삽입
                int num = 0;
                for (int j = 0; j < n / 4; j++) {
                    num += hex[i + side * (n / 4) + j] * pow(16, j);
                }
                mxk.insert(num);
            }
        }
        set<int, greater<int>>::iterator it = mxk.begin();
        for (int i = 1; i < k; i++) {
            it++;
        }

        cout << '#' << test_case << ' ' << *it << '\n';
        mxk.clear();
    }
    return 0;
}